from math import sqrt, factorial

# Saves on memory as only needed components of a module are imported
# Multiple components can be imported at once
root = sqrt(9)
print(root)
factorial_10 = factorial(10)
print(factorial_10)